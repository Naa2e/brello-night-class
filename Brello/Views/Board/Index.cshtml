@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    var things = new List<string>();
    things.Add("foo");
    things.Add("bar");
}

    <h1>@ViewBag.Message</h1>
    @using (Html.BeginForm("Create","Board", FormMethod.Post, htmlAttributes: new {style = "display:block;", @class = "forgot" })) {
        @Html.AntiForgeryToken();
        @Html.TextBox("Name");
}

<ul>
    @foreach (var item in ViewBag.Things)
    {
        <li>@item</li>

    }
</ul>
    
    